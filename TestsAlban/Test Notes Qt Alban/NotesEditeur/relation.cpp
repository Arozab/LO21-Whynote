#include "relation.h"
using namespace std;


/*void relation::addCouple(string l, int n1, int n2) {
    if (!existe_relation(n1,n2)) {
        ensemble_couple.push_back(new couple(l,n1,n2));
    }
}

void relation::removeCouple(Notes& n1, Notes& n2) {
    if (existe_relation(n1,n2)) {
        string labl;
        int indice,k;
        k=0;
        for (auto couples : ensemble_couple) {
            if (couples->getNotes1()==&n1 && couples->getNotes2()==&n2) {
                indice=k;
                break;
            }
            k++;
        }
        ensemble_couple.erase(ensemble_couple.begin()+indice);
    }
}*/
